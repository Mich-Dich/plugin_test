{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake Build (Debug)",
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build",
                "${workspaceFolder}/build",
                "--parallel"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$gcc",
            "detail": "Build the Gluttony target in Debug mode"
        },
        {
            "label": "CMake Clean Build (Debug)",
            "type": "shell",
            "command": "cmake -E remove_directory build && cmake -B build -DCMAKE_BUILD_TYPE=debug && cmake --build build --parallel",
            "group": "build",
            "problemMatcher": "$gcc",
            "detail": "Remove build dir, configure Debug, then build (cross-platform)"
        }
    ]
}